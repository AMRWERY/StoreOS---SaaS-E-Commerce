/** CRUD for `page_sections` – replace with Supabase/API calls. */
export function usePageSections() {
  return {
    list: async (_pageId: string) => [],
    create: async () => null,
    update: async () => null,
    remove: async () => null,
    reorder: async () => null,
  }
}
