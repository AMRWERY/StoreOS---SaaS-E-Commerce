{
  "buildCommand": "cd ../.. && pnpm turbo build --filter=@storeos/storefront --force",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nuxtjs",
  "outputDirectory": ".output"
}
