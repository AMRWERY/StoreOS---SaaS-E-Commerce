{
  "name": "@storeos/admin",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port=3001",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "lint": "nuxt typecheck",
    "clean": "rm -rf .nuxt .output",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@storeos/core": "workspace:*",
    "@storeos/ui": "workspace:*",
    "nuxt": "^4.4.2",
    "vue": "^3.5.30",
    "vue-router": "^5.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
