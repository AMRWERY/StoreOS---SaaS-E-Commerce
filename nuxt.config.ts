// This is a reference-only root config.
// Shared module configuration lives in layers/core/nuxt.config.ts
// Each app extends it via: extends: ["@storeos/ui", "@storeos/core"]
export default defineNuxtConfig({})
